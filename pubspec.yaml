name: medical_app
description: A medical application developed by Karim Asaad Qabil.
authors:
  - Karim Asaad Qabil <your_email@example.com> # Replace with your actual email if desired
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  firebase_storage: ^11.6.9
  firebase_messaging: ^14.8.6
  cached_network_image: ^3.3.1
  intl: ^0.20.2
  flutter_screenutil: ^5.9.0
  flutter_animate: ^4.5.0
  safe_device: ^1.1.1
  http_certificate_pinning: ^3.0.1
  package_info_plus: ^8.0.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/

#  fonts:
#    - family: Cairo
#      fonts:
#        - asset: assets/fonts/Cairo-Regular.ttf
#        - asset: assets/fonts/Cairo-Bold.ttf
#          weight: 700
